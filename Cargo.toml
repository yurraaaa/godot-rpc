[package]
name = "godot-rpc"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true

[dependencies]
discord-rich-presence = "1.1.0"
godot = "0.5"